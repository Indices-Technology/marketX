# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests\e2e\commerce\checkout.spec.ts >> Commerce — Cart & Checkout >> Guest — inline auth step >> "Continue" button enables once a valid email is typed
- Location: tests\e2e\commerce\checkout.spec.ts:23:5

# Error details

```
Error: expect(locator).toBeEnabled() failed

Locator:  getByRole('button', { name: /continue/i })
Expected: enabled
Received: disabled
Timeout:  15000ms

Call log:
  - Expect "toBeEnabled" with timeout 15000ms
  - waiting for getByRole('button', { name: /continue/i })
    18 × locator resolved to <button disabled data-v-c6f0ec98="" class="mt-4 flex w-full items-center justify-center gap-2 rounded-xl bg-brand py-3 text-sm font-bold text-white transition-colors hover:bg-brand/90 disabled:opacity-50">…</button>
       - unexpected value "disabled"

```

# Page snapshot

```yaml
- generic [ref=e4]:
  - complementary [ref=e5]:
    - generic [ref=e6]:
      - link "MX MarketX" [ref=e7] [cursor=pointer]:
        - /url: /
        - generic [ref=e9]: MX
        - generic [ref=e10]: MarketX
      - navigation [ref=e11]:
        - link "Home" [ref=e12] [cursor=pointer]:
          - /url: /
          - generic [ref=e14]: Home
        - link "Discover" [ref=e15] [cursor=pointer]:
          - /url: /discover
          - generic [ref=e17]: Discover
        - link "Reels" [ref=e18] [cursor=pointer]:
          - /url: /reels
          - generic [ref=e20]: Reels
        - link "Near Me" [ref=e21] [cursor=pointer]:
          - /url: /map
          - generic [ref=e23]: Near Me
        - link "Squares" [ref=e24] [cursor=pointer]:
          - /url: /squares
          - generic [ref=e26]: Squares
      - navigation [ref=e28]:
        - link "Create" [ref=e29] [cursor=pointer]:
          - /url: /user-register
          - generic [ref=e31]: Create
        - button "Cart" [ref=e32] [cursor=pointer]:
          - generic [ref=e35]: Cart
      - link "Sign In" [ref=e37] [cursor=pointer]:
        - /url: /user-login
        - generic [ref=e39]: Sign In
  - main [ref=e40]:
    - generic [ref=e44]:
      - generic [ref=e45]:
        - link [ref=e46] [cursor=pointer]:
          - /url: /
        - heading "Checkout" [level=1] [ref=e48]
      - generic [ref=e49]:
        - generic [ref=e53]:
          - paragraph [ref=e54]: Who's buying?
          - paragraph [ref=e55]: We'll create an account for you automatically if you're new.
        - generic [ref=e56]:
          - textbox "Email address" [active] [ref=e57]
          - textbox "Full name (for your order)" [ref=e58]
          - textbox "Phone number (optional)" [ref=e59]
        - button "Continue" [disabled] [ref=e60]
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test'
  2   | import { pageLogin, apiLogin, clearCart, getFirstVariantId } from '../../helpers/auth'
  3   | 
  4   | const PRODUCT_SLUG = 'adire-tie-dye-maxi-dress'
  5   | const T = { timeout: 15000 }
  6   | 
  7   | test.describe('Commerce — Cart & Checkout', () => {
  8   |   // ── Guest checkout ─────────────────────────────────────────────────────────
  9   |   test.describe('Guest — inline auth step', () => {
  10  |     test('guest sees the inline auth form on /checkout', async ({ page }) => {
  11  |       await page.goto('/checkout')
  12  |       await expect(page.getByRole('heading', { name: /checkout/i })).toBeVisible(T)
  13  |       await expect(page.getByText(/who's buying/i)).toBeVisible(T)
  14  |       await expect(page.locator('input[type="email"]').first()).toBeVisible()
  15  |     })
  16  | 
  17  |     test('"Continue" button is disabled until email is entered', async ({ page }) => {
  18  |       await page.goto('/checkout')
  19  |       await expect(page.getByText(/who's buying/i)).toBeVisible(T)
  20  |       await expect(page.getByRole('button', { name: /continue/i })).toBeDisabled(T)
  21  |     })
  22  | 
  23  |     test('"Continue" button enables once a valid email is typed', async ({ page }) => {
  24  |       await page.goto('/checkout')
  25  |       await expect(page.getByText(/who's buying/i)).toBeVisible(T)
  26  |       // pressSequentially fires real keyboard events — more reliable for Vue v-model than fill()
  27  |       await page.locator('input[type="email"]').first().pressSequentially('guest@example.com')
> 28  |       await expect(page.getByRole('button', { name: /continue/i })).toBeEnabled(T)
      |                                                                     ^ Error: expect(locator).toBeEnabled() failed
  29  |     })
  30  | 
  31  |     test('clearing the email disables the Continue button again', async ({ page }) => {
  32  |       await page.goto('/checkout')
  33  |       await expect(page.getByText(/who's buying/i)).toBeVisible(T)
  34  |       const emailInput = page.locator('input[type="email"]').first()
  35  |       await emailInput.pressSequentially('guest@example.com')
  36  |       await expect(page.getByRole('button', { name: /continue/i })).toBeEnabled(T)
  37  |       // fill('') fires input event and clears the field
  38  |       await emailInput.fill('')
  39  |       await expect(page.getByRole('button', { name: /continue/i })).toBeDisabled(T)
  40  |     })
  41  |   })
  42  | 
  43  |   // ── Authenticated checkout ─────────────────────────────────────────────────
  44  |   test.describe('Authenticated — checkout flow', () => {
  45  |     test('shows checkout heading and no inline-auth step', async ({ page, request }) => {
  46  |       await pageLogin(page, request)
  47  |       await page.goto('/checkout')
  48  |       await expect(page.getByRole('heading', { name: /checkout/i })).toBeVisible(T)
  49  |       // Auth step must not appear for a logged-in user
  50  |       await expect(page.getByText(/who's buying/i)).not.toBeVisible()
  51  |     })
  52  | 
  53  |     test('checkout page has correct URL', async ({ page, request }) => {
  54  |       await pageLogin(page, request)
  55  |       await page.goto('/checkout')
  56  |       await expect(page.getByRole('heading', { name: /checkout/i })).toBeVisible(T)
  57  |       await expect(page).toHaveURL('/checkout')
  58  |     })
  59  | 
  60  |     test('checkout is accessible after seeding cart via API', async ({ page, request }) => {
  61  |       const { token } = await apiLogin(request)
  62  |       await clearCart(request, token)
  63  |       const variantId = await getFirstVariantId(request)
  64  |       await request.post('/api/commerce/cart', {
  65  |         data: { variantId, quantity: 1 },
  66  |         headers: { Authorization: `Bearer ${token}` },
  67  |       })
  68  | 
  69  |       await pageLogin(page, request)
  70  |       await page.goto('/checkout')
  71  |       await expect(page.getByRole('heading', { name: /checkout/i })).toBeVisible(T)
  72  |     })
  73  |   })
  74  | 
  75  |   // ── Add to cart interaction ────────────────────────────────────────────────
  76  |   test.describe('Add to cart', () => {
  77  |     test('authenticated user can add a product and gets no error', async ({
  78  |       page,
  79  |       request,
  80  |     }) => {
  81  |       const { token } = await apiLogin(request)
  82  |       await clearCart(request, token)
  83  | 
  84  |       await pageLogin(page, request)
  85  |       await page.goto(`/product/${PRODUCT_SLUG}`)
  86  | 
  87  |       const addBtn = page.getByRole('button', { name: /add to cart/i })
  88  |       await expect(addBtn).toBeEnabled(T)
  89  |       await addBtn.click()
  90  | 
  91  |       await expect(page.locator('body')).not.toContainText('Something went wrong')
  92  |       await expect(page.locator('body')).not.toContainText('error', { ignoreCase: true })
  93  |     })
  94  | 
  95  |     test('add-to-cart briefly shows "Adding…" state', async ({ page, request }) => {
  96  |       const { token } = await apiLogin(request)
  97  |       await clearCart(request, token)
  98  | 
  99  |       await pageLogin(page, request)
  100 |       await page.goto(`/product/${PRODUCT_SLUG}`)
  101 | 
  102 |       const addBtn = page.getByRole('button', { name: /add to cart/i })
  103 |       await expect(addBtn).toBeEnabled(T)
  104 | 
  105 |       // Click and immediately assert the in-flight state
  106 |       const [_, addingText] = await Promise.all([
  107 |         addBtn.click(),
  108 |         page
  109 |           .getByRole('button', { name: /adding…/i })
  110 |           .waitFor({ timeout: 3000 })
  111 |           .catch(() => null), // may resolve too fast on fast connections
  112 |       ])
  113 |       // After API call: button should revert (no crash)
  114 |       await expect(page.locator('body')).not.toContainText('Something went wrong')
  115 |     })
  116 |   })
  117 | 
  118 |   // ── Order success page ─────────────────────────────────────────────────────
  119 |   test.describe('/success', () => {
  120 |     test('renders "Thank You for Your Order!" heading', async ({ page }) => {
  121 |       await page.goto('/success')
  122 |       await expect(
  123 |         page.getByRole('heading', { name: /thank you for your order/i }),
  124 |       ).toBeVisible(T)
  125 |     })
  126 | 
  127 |     test('shows the "What\'s Next?" section', async ({ page }) => {
  128 |       await page.goto('/success')
```